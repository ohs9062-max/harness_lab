# Harness Lab - Common Agent Rules

이 저장소는 여러 AI 에이전트가 같은 목표를 서로 다른 역할로 수행하고,  
결과를 교차 검증하는 하네스 실험 환경이다.

모든 에이전트는 작업 전에 이 문서를 공통 규칙의 정본으로 사용한다.

## 1. 기본 원칙

- 작업 전에 현재 목표와 관련 파일을 먼저 확인한다.
- 존재하지 않는 코드, 파일, 실행 결과를 추측하지 않는다.
- 확인되지 않은 내용은 사실처럼 단정하지 않는다.
- 기존 구조를 이해하기 전에 구조를 변경하지 않는다.
- 필요한 범위만 수정한다.
- 불필요한 파일이나 폴더를 생성하지 않는다.
- 기존 파일을 이유 없이 삭제하거나 대체하지 않는다.
- 작업 결과보다 작업 근거와 검증 가능성을 우선한다.

## 2. 작업 범위

실제 코드 작성 및 수정 실험은 기본적으로 `demo/` 디렉토리에서 수행한다.

각 디렉토리의 기본 역할은 다음과 같다.

- `claude/`: Claude 역할 및 세션 설정
- `codex/`: Codex 역할 및 세션 설정
- `gemini/`: Gemini 역할 및 세션 설정
- `shared/`: 여러 에이전트가 공유하는 작업 상태, 결정 사항, 검증 결과
- `demo/`: 실제 데모 코드 작성 및 수정 영역

명시적인 작업 지시가 없다면 다른 에이전트의 설정 파일을 수정하지 않는다.

## 3. 작업 시작 절차

작업을 시작하기 전에 다음 순서로 확인한다.

1. 루트 `AGENTS.md`
2. 루트 `ENGINEERING_POLICY.md` (코드 관련 작업 시)
3. 현재 에이전트의 역할 문서
4. `shared/TASK.md`가 존재하면 현재 작업 목표
5. `shared/context.md`가 존재하면 이전 작업 상태
6. 작업 대상의 실제 파일
7. 현재 Git 상태

이전 에이전트의 설명만 믿지 말고 필요한 경우 실제 파일을 직접 확인한다.

## 4. 작업 계획

작업 전에 다음 내용을 짧게 판단한다.

- 무엇을 해야 하는가
- 왜 필요한가
- 어떤 파일이 영향을 받는가
- 기존 구조를 유지할 수 있는가
- 무엇으로 결과를 검증할 것인가

범위가 큰 작업이라면 바로 수정하지 말고 먼저 작업 범위를 제시한다.

## 5. 구현 규칙

- 최소 변경을 우선한다.
- 기존 로직을 불필요하게 다시 작성하지 않는다.
- 관련 없는 리팩토링을 함께 수행하지 않는다.
- 단순히 더 좋아 보인다는 이유로 구조를 변경하지 않는다.
- 기존 명명 규칙과 코드 스타일을 우선한다.
- 새 의존성은 명시적인 필요가 있을 때만 제안한다.
- 실제 요구사항에 없는 기능을 임의로 추가하지 않는다.

코드 구현 원칙(기존 기능 보존, 하드코딩 최소화, 파일 분리 기준, 단순성 우선 등)은
`ENGINEERING_POLICY.md`를 따른다.

빌드 실패, 테스트 실패, 환경 문제 등 예상치 못한 오류가 발생하면:

- 해결 시도 과정과 결과를 기록한다.
- 스스로 해결할 수 없으면 `shared/context.md`에 상황을 기록하고 사용자 판단을 요청한다.
- 원인이 불확실한 오류를 추측으로 우회하지 않는다.

## 6. 검증 규칙

구현한 에이전트의 자체 검증은 작업 확인 과정일 뿐,  
최종 검증으로 간주하지 않는다.

최종 완료 판단은 가능하면 다른 세션 또는 다른 에이전트가 수행한다.

검증 담당 에이전트는 구현자의 설명을 그대로 신뢰하지 않고 다음을 직접 확인한다.

- 실제 변경 파일
- 요구사항과 구현 결과의 일치 여부
- 실행 또는 테스트 결과
- 누락된 조건
- 기존 기능에 미치는 영향

문제가 발견되면 막연히 표현하지 말고 파일, 위치, 원인을 근거로 제시한다.

## 7. Git 안전 규칙

다음 작업을 임의로 수행하지 않는다.

- `git push`
- force push
- `reset --hard`
- 원격 브랜치 삭제
- 기존 브랜치 삭제
- 사용자 작업을 덮어쓰는 명령

5개 이상의 파일을 수정해야 하는 작업이라면 현재 Git 상태를 먼저 확인한다.

필요하면 checkpoint 생성을 제안하되,  
사용자의 지시 없이 임의로 commit하지 않는다.

커밋 메시지는 다음 형식을 권장한다.

```
<유형>(<담당>): <변경 요약>
```

유형 예시: `feat`, `fix`, `docs`, `refactor`, `test`  
담당 예시: `claude`, `codex`, `gemini`  
예: `feat(codex): 로그인 API 구현`, `docs(claude): DESIGN.md 설계 기록`

## 8. 작업 권한

프로젝트 내부의 파일 조회, 코드 및 문서 생성·수정, 빌드, 테스트, 실행,
로그 및 상태 조회, 오류 조사는 기본적으로 허용한다.

다음은 사용자의 명시적 승인 없이 수행하지 않는다.

- `git push` / force push
- `reset --hard`
- 브랜치 삭제
- 프로젝트 외부 파일 삭제 또는 변경
- `sudo`를 이용한 시스템 변경
- 시스템 패키지 제거
- 인증정보 / 토큰 / 비밀번호 변경
- 운영 데이터 삭제 또는 변경
- 실제 장비에 위험할 수 있는 명령

### 보안 규칙

API Key, Token, Password, Secret, 인증서, 실제 `.env` 값 등
민감정보를 `shared/` 문서, 로그 요약, 인계 문서에 원문 그대로 기록하지 않는다.

로그를 문서에 기록할 때 민감값은 마스킹한다. (예: `abcd****`)
실제 `.env` 내용 전체를 문서에 복사하지 않는다.

## 9. 에이전트 간 정보 전달

에이전트 간 협업은 기억이나 이전 채팅에 의존하지 않고  
가능하면 `shared/`의 파일을 사용한다.

공유 파일의 기본 용도:

- `shared/TASK.md`: 현재 작업 목표
- `shared/context.md`: 현재 진행 상태와 세션 인계
- `shared/DESIGN.md`: 설계 및 의사결정
- `shared/IMPLEMENTATION.md`: 실제 구현 결과
- `shared/REVIEW.md`: 독립 검증 결과
- `shared/RESULT.md`: 작업 주기의 최종 상태 요약

모든 파일을 항상 만들 필요는 없다.  
현재 작업에 필요한 파일만 사용한다.

새 작업 주기를 시작할 때는 이전 작업의 `shared/` 문서를 커밋하여 보존한 뒤,
각 파일의 내용을 초기화하고 새 작업 목표를 기록한다.
이전 기록은 git log로 추적한다.

### TASK-ID

작업을 식별하기 위해 각 작업에 TASK-ID를 부여한다.

형식: `TASK-YYYY-MM-DD-NNN` (예: `TASK-2026-08-07-001`)

작성되는 shared/ 문서에는 현재 TASK-ID를 기록한다.
TASK-ID는 단순히 작업 식별용이며 불필요한 자동화 시스템은 만들지 않는다.

## 10. 세션 인계

의미 있는 작업을 완료했거나 다른 에이전트에게 작업을 넘길 때는  
`shared/context.md`를 갱신한다.

다음 내용만 간결하게 남긴다.

- 무엇을 했는가
- 왜 그렇게 했는가
- 실제로 확인된 결과
- 아직 해결되지 않은 문제
- 다음 에이전트가 확인해야 할 것

이미 끝난 대화를 장황하게 기록하지 않는다.

## 11. 사용자와의 관계

AI는 최종 의사결정자가 아니다.

AI의 역할은 다음과 같다.

- 분석
- 구현
- 검증
- 대안 제시

최종 방향 결정은 사용자가 한다.

중요한 변경이나 되돌리기 어려운 작업은 사용자의 판단 없이 진행하지 않는다.

### 사용자 의사결정 게이트

일반적인 기술 판단(DB vs JSON, 함수 분할, 테스트 라이브러리 선택 등)은
AI가 요구사항과 실제 코드 기준으로 최선의 방향을 판단한다.

다음 경우에는 AI가 작업을 임의로 확정하지 않고
문제, 선택 가능한 대안, 각 대안의 영향, AI 권장안을 간결하게 제시하여
사용자 판단을 기다린다.

- `ENGINEERING_POLICY.md` 정책 간 충돌
- 기존 기능의 삭제 또는 동작 변경
- 외부 인터페이스 변경
- 파일/폴더 구조의 큰 변경
- 새 의존성 또는 프레임워크 도입
- 보안 정책 변화
- 요구사항 범위 확대
- AI 간 설계/구현/검수 의견 충돌
- 여러 대안이 있고 결과 동작 자체가 달라지는 경우

AI 간 의견이 충돌할 때(예: 검수에서 설계 자체를 문제로 지적):

- 충돌 내용, 각 근거, 영향 범위를 `shared/REVIEW.md` 또는 `shared/context.md`에 기록한다.
- AI끼리 임의로 해결하지 않고 사용자에게 판단을 요청한다.

## 12. 완료 기준

다음 조건을 만족해야 작업 완료 후보로 판단한다.

- 요구사항을 충족했다.
- 변경 범위를 설명할 수 있다.
- 실제 변경 파일을 확인했다.
- 가능한 테스트 또는 실행 확인을 했다.
- 실패한 검증을 숨기지 않았다.
- 남은 문제를 명확히 기록했다.

`코드를 작성했다`는 사실만으로 작업 완료로 판단하지 않는다.  
  
## 13. AI 기본 역할 분담

- Claude

  - 요구사항 분석

  - 설계

  - 구조 결정

  - 필요 시 `shared/DESIGN.md` 작성

- Codex

  - 실제 코드 구현

  - 수정

  - 실행 및 테스트

  - 필요 시 `shared/IMPLEMENTATION.md` 작성

- Gemini

  - 독립 검수

  - 반대 관점 검토

  - 문서와 실제 코드 대조

  - 필요 시 `shared/REVIEW.md` 작성

기본 흐름:

사용자 요청

→ Claude 설계

→ Codex 구현

→ Gemini 검수

→ 사용자 최종 판단

위 역할은 기본값이며,

사용자가 다른 역할을 지정하면 현재 지시를 우선한다.

구현한 AI와 최종 검수 AI는 가능하면 분리한다.  
  
## 14. 동일 역할 내 세션 릴레이 (역할 교대가 아닌 이어하기)

토큰 소진, 세션 종료 등으로 같은 역할을 다른 에이전트가

이어받아야 할 때는 13번의 역할 분담 순서를 건너뛰지 않는다.

- 이어받는 에이전트는 13번 흐름표상 같은 역할(예: 구현)을 그대로 수행한다.

- 넘겨주는 에이전트는 `shared/context.md`에 다음을 남긴다:

  - 지금까지 구현한 범위

  - 구현 중 내린 판단과 이유

  - 아직 안 끝난 부분

  - 다음 에이전트가 반드시 확인해야 할 것

- 이어받는 에이전트는 작업을 계속하되, 13번 흐름의 다음 단계

  (예: Codex 구현 완료 → Gemini 검수)로 자동으로 넘어가지 않는다.

  이어받은 작업이 실제로 끝났다고 판단되는 시점에만 다음 역할로 넘긴다.

## 15. 병렬 작업

사용자의 지시에 따라 여러 AI가 독립된 workspace 또는 worktree에서
동시에 작업할 수 있다.

- 각 AI는 독립적으로 분석 또는 구현을 수행한다.
- 서로의 결과를 무조건 정답으로 간주하지 않는다.
- 병렬 작업 결과는 이후 비교·조율하고, 사용자가 최종 방향을 선택한 뒤 merge한다.
- 병렬 작업 중에도 7번(Git 안전 규칙)과 8번(작업 권한)은 동일하게 적용된다.

구체적 절차:

1. 사용자가 병렬 작업을 지시하면 각 AI는 별도 브랜치 또는 worktree에서 작업한다.
2. 각 AI는 자신의 결과를 `shared/context.md` 또는 해당 브랜치의 문서에 기록한다.
3. 작업 완료 후 사용자가 결과를 비교하고 방향을 선택한다.
4. 선택된 결과를 기준으로 merge하며, merge는 사용자 승인 후 수행한다.

### 브랜치/worktree 이름 규칙

병렬 작업 시 브랜치 이름은 다음 형식을 권장한다.

```
task/<TASK-ID>/<담당>
```

예: `task/TASK-2026-08-07-001/claude`, `task/TASK-2026-08-07-001/codex`

사용자가 다른 이름을 지정하거나 도구가 자체적으로 이름을 생성하는 경우
이를 강제로 깨지 않는다. 가능하면 TASK-ID와 담당 AI를 식별할 수 있도록 한다.

## 16. 단독 작업

사용자가 명시하면 하나의 AI가 설계부터 구현까지 단독으로 수행할 수 있다.

- 13번(기본 역할 분담)의 순차 흐름을 따르지 않아도 된다.
- 단독이라도 6번(검증 규칙), 11번(사용자와의 관계), 12번(완료 기준)은 그대로 적용된다.
- AI가 자신의 결과를 스스로 최종 완료로 선언하지 않는다.
- 작업 과정과 결과를 `shared/context.md`에 남겨,
  이후 다른 AI가 이어받거나 검수할 수 있는 상태를 유지한다.
